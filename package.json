{
  "name": "@diegofrayo/code",
  "version": "1.0.0",
  "description": "monorepo that contains some own JavaScript packages and their docs",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build:docs": "node build.js --build-docs",
    "build:all": "node build.js --build-all",
    "lint:all": "node build.js --lint-all"
  },
  "dependencies": {
    "microbundle": "^0.11.0",
    "shelljs": "^0.8.3",
    "yargs": "^13.2.2"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
